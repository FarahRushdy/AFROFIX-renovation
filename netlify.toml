[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — also covered by public/_redirects, kept here for clarity
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
